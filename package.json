{
  "name": "seer2-next-client",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . seer2-next-client --platform=win32 --out ./dist --overwrite",
    "pack": "electron-builder --dir && node build.js",
    "dist": "electron-builder --win",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ukuq",
  "license": "MIT",
  "description": "seer2",
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-builder": "^24.13.3",
    "electron-forge": "^5.2.4",
    "electron-packager": "^17.1.2",
    "electron-prebuilt-compile": "8.2.0"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
